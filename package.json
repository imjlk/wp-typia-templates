{
  "name": "wp-typia",
  "version": "1.0.0",
  "description": "Bun-first WordPress block templates with Typia-powered metadata generation",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "packages/create",
    "packages/create-wp-typia",
    "packages/wp-typia-block-types",
    "test-template/*"
  ],
  "scripts": {
    "build": "bun run --filter @wp-typia/block-types build && bun run --filter @wp-typia/create build && bun run --filter my-typia-block build",
    "test": "bun run test:unit && bun run test:cli",
    "test:unit": "bun test tests/unit",
    "test:cli": "bun run --filter @wp-typia/create test",
    "test:generated-smoke": "node scripts/run-generated-project-smoke.mjs",
    "test:coverage": "bun run test:unit:coverage && bun run test:cli:coverage",
    "test:unit:coverage": "bun test tests/unit --coverage --coverage-reporter=text --coverage-reporter=lcov --coverage-dir=coverage/unit",
    "test:cli:coverage": "bun run --filter @wp-typia/create test:coverage",
    "test:watch": "bun test tests/unit --watch",
    "test:e2e": "bun run --filter my-typia-block build && playwright test",
    "test:e2e:matrix": "bun run --filter my-typia-block build && PLAYWRIGHT_MATRIX=1 playwright test",
    "lint": "bun run --filter my-typia-block --if-present lint",
    "format": "bun run --filter my-typia-block --if-present format",
    "clean": "bun run --workspaces --if-present clean && rm -rf node_modules",
    "dev": "bun run --filter my-typia-block --if-present dev",
    "typecheck": "tsc --noEmit",
    "release": "sampo release",
    "publish": "sampo publish",
    "publish:oidc": "bash scripts/publish-oidc.sh",
    "sampo:add": "sampo add",
    "docs:dev": "typedoc --watch",
    "docs:build": "typedoc",
    "wp-env:start": "bunx --package @wordpress/env wp-env start",
    "wp-env:stop": "bunx --package @wordpress/env wp-env stop",
    "wp-env:reset": "bunx --package @wordpress/env wp-env destroy all && bunx --package @wordpress/env wp-env start"
  },
  "keywords": [
    "wordpress",
    "gutenberg",
    "block",
    "typia",
    "typescript",
    "template",
    "monorepo"
  ],
  "author": "imjlk",
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/imjlk/wp-typia.git"
  },
  "bugs": {
    "url": "https://github.com/imjlk/wp-typia/issues"
  },
  "homepage": "https://github.com/imjlk/wp-typia#readme",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.9.0",
    "@types/wordpress__block-editor": "^11.5.17",
    "@types/wordpress__blocks": "^12.5.18",
    "@types/wordpress__components": "^23.8.0",
    "@wordpress/browserslist-config": "^6.42.0",
    "@types/wordpress__element": "^2.14.1",
    "@types/wordpress__i18n": "^3.11.0",
    "bun-types": "^1.3.11",
    "typedoc": "^0.25.4",
    "typedoc-plugin-markdown": "^3.16.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "typia": "^12.0.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0",
    "bun": ">=1.3.10"
  }
}
