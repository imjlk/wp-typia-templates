/**
 * Public runtime primitive helpers shared across wp-typia packages.
 * This module exposes the supported helper surface without requiring consumers
 * to depend on the internal-only subpath.
 * @module
 */
export * from "./internal/runtime-primitives.js";
