import { tags } from 'typia';

export interface {{pascalCase}}ItemAttributes {
	title: string &
		tags.MinLength< 1 > &
		tags.MaxLength< 80 > &
		tags.Default< '{{compoundChildTitle}}' >;
	body: string &
		tags.MinLength< 1 > &
		tags.MaxLength< 280 > &
		tags.Default< 'Add supporting details for this internal item.' >;
}
