# Persistence Examples

Runtime reference plugin for the `persistence` scaffold family.

- `Persistence Counter`: public aggregate counter backed by signed public write tokens
- `Persistence Like Button`: authenticated per-user toggle like button
- `WordPress AI Projections`: repo-local proof that the counter contracts can
  drive both AI Client structured responses and Abilities API registration
