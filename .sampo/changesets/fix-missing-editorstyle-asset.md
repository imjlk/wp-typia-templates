---
npm/@wp-typia/create: patch
---

Remove generated `editorStyle` metadata entries that pointed at a non-existent `index.css` asset, and harden scaffold smoke checks so built `block.json` file references stay aligned with emitted assets.
