---
npm/@wp-typia/create: minor
---

Add read-only `migrations plan` and TTY-guided `migrations wizard` commands so migration-capable projects can preview one selected legacy edge before scaffolding rules or fixtures.
