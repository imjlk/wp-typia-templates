---
npm/@wp-typia/create: minor
---

Replace semver-based migration workspace labels with explicit `vN` schema migration versions across the CLI, generated config, migration UI defaults, and migration docs.
