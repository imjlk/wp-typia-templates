---
npm/wp-typia: patch
---

Upgraded the repo-owned lint stack to `eslint` 9 with `@typescript-eslint` 8,
added root `lint:fix` and `format:write` scripts, and documented the
maintainer autofix flow alongside the formatter/lint baseline validator.
