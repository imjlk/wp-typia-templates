---
npm/@wp-typia/create: patch
---

Improve `@wp-typia/create` hot-path performance for repeated metadata sync
flows, restore the CLI runtime barrel compatibility export, and tighten the
runtime benchmark and CLI helper behavior around quoted next steps.
